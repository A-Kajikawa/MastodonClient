package com.s24.mastodonclient.ui.toot_list

import com.s24.mastodonclient.R

enum class TimelineType(private val id: Int) {
    HomeTimeline(R.id.menu_home),

    PublicTimeline(R.id.menu_public);
}